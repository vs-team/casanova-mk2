module Pippo

open "MonoGame.Framework.dll"
open Microsoft.Xna.Framework

worldEntity World = {
  Cnt       : Vector2
  rule Cnt = Cnt + Vector2.One 
  rule Cnt = Cnt + Vector2.One
  Create() =
    {
      Cnt = Vector2.One
    }
}
